{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "pnpm install && pnpm -w run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite"
}
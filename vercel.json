{
  "rewrites": [
    {
      "source": "/((?!assets/|.*\\..*).*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "pnpm install && pnpm -w run build",
  "outputDirectory": "dist",
  "framework": "vite"
}

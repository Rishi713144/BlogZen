{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ],
  "buildCommand": "pnpm install && pnpm -w run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite"
}

